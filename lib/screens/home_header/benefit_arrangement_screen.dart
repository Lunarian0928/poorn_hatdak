import 'package:flutter/material.dart';

class BenefitArrangementScreen extends StatelessWidget {
  const BenefitArrangementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '혜택 정리',
      ),
    );
  }
}
