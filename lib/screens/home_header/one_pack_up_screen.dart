import 'package:flutter/material.dart';

class OnePackUpScreen extends StatelessWidget {
  const OnePackUpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '1팩 담기',
      ),
    );
  }
}
