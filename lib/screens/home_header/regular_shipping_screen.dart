import 'package:flutter/material.dart';

class RegularShippingScreen extends StatelessWidget {
  const RegularShippingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '정기 배송',
      ),
    );
  }
}
