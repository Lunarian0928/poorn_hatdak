import 'package:flutter/material.dart';

class BargainPriceScreen extends StatelessWidget {
  const BargainPriceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '이달의 특가',
      ),
    );
  }
}
