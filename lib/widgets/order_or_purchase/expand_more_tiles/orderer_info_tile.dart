import '../../../classes/expand_more_tile.dart';

class OrdererInfoTile extends ExpandMoreTile {
  const OrdererInfoTile({
    super.key,
    super.title = '배송자 정보',
    super.expandMoreBtnLabel = '주문자',
  });
}
